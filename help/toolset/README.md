# Context Loader 工具集（ADP）

`context_loader_toolset.adp` 与源码中的发布物一致：

`adp/context-loader/agent-retrieval/docs/release/toolset/context_loader_toolset.adp`

此处副本便于在文档或示例中引用固定路径。`deploy.sh` 在安装 Core 后导入工具集时，默认仍从 `adp/...` 路径读取（可用 `CONTEXT_LOADER_TOOLSET_ADP_PATH` 覆盖）。
