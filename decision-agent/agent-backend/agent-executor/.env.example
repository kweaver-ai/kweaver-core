# 配置文件路径
AGENT_EXECUTOR_CONFIG_PATH=./conf/

# 是否提取证据字段（从工具响应中提取 nodes 等数据作为 _evidence）
ENABLE_EVIDENCE_EXTRACTION=false

# 是否将内置 skill 使用规则（_SKILL_USAGE_RULES）拼接到系统提示词
# 默认  true；设置为 true 时才拼接
skill_enabled=true
