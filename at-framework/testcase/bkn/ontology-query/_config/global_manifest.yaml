variables:
- name: kn_id
  description: knowledge network id
  ref: ${kn_id}
- name: object_id
  description: object type id
  ref: ${object_id}
- name: action_id
  description: action type id
  ref: ${action_id}
- name: groups_id
  description: concept group id
  ref: ${groups_id}
- name: objects_id
  description: object id in group
  ref: ${objects_id}
- name: ot_id
  description: object type id for query
  ref: ${ot_id}
- name: id
  description: generic id
  ref: ${id}
- name: rt_ids
  description: relation type ids
  ref: ${rt_ids}
