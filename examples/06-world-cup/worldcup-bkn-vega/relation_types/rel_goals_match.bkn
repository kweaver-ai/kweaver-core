---
type: relation_type
id: rel_goals_match
name: 进球事件·场次→比赛场次
tags: [worldcup, fjelstul, backbone]
---

## RelationType: 进球事件·场次→比赛场次

**语义**：在「进球事件」事实中，字段 `match_id` 引用「比赛场次」的主键 `match_id`，表示该事件或统计发生在哪一场比赛。

**建模用途**：多表联接、Agent 问答与统计时的 join 路径；请同时利用同名 `_name` 列做可读展示。

**技术映射**：`goals.match_id` → `matches.match_id`（direct）。

### Endpoint

| Source | Target | Type |
|--------|--------|------|
| goals | matches | direct |

### Mapping Rules

| Source Property | Target Property |
|-----------------|-----------------|
| match_id | match_id |
