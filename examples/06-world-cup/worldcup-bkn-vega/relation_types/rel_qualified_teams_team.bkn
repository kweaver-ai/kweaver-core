---
type: relation_type
id: rel_qualified_teams_team
name: 出线球队资格·球队→国家队
tags: [worldcup, fjelstul, backbone]
---

## RelationType: 出线球队资格·球队→国家队

**语义**：在「出线球队资格」事实中，字段 `team_id` 引用「国家队」的主键 `team_id`，表示所指的国家队主体。

**建模用途**：多表联接、Agent 问答与统计时的 join 路径；请同时利用同名 `_name` 列做可读展示。

**技术映射**：`qualified_teams.team_id` → `teams.team_id`（direct）。

### Endpoint

| Source | Target | Type |
|--------|--------|------|
| qualified_teams | teams | direct |

### Mapping Rules

| Source Property | Target Property |
|-----------------|-----------------|
| team_id | team_id |
