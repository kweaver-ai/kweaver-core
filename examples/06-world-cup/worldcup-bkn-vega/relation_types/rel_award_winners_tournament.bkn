---
type: relation_type
id: rel_award_winners_tournament
name: 奖项得主·届次赛事→世界杯赛事
tags: [worldcup, fjelstul, backbone]
---

## RelationType: 奖项得主·届次赛事→世界杯赛事

**语义**：在「奖项得主」事实中，字段 `tournament_id` 引用「世界杯赛事」的主键 `tournament_id`，表示该记录隶属于哪一届世界杯赛事。

**建模用途**：多表联接、Agent 问答与统计时的 join 路径；请同时利用同名 `_name` 列做可读展示。

**技术映射**：`award_winners.tournament_id` → `tournaments.tournament_id`（direct）。

### Endpoint

| Source | Target | Type |
|--------|--------|------|
| award_winners | tournaments | direct |

### Mapping Rules

| Source Property | Target Property |
|-----------------|-----------------|
| tournament_id | tournament_id |
