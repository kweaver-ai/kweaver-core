# 06-world-cup 排障备忘

此前的 **Dataview 分支流水线**（`run-branch-bkn.sh`、`run-bkn-template.sh`、`worldcup-bkn/` 等）已从本示例移除。

当前推荐路径：

1. **一键 Demo**：[`../README.zh.md`](../README.zh.md) · `./download.sh` + `./run.sh`（`create-from-csv`）。
2. **Vega Catalog + 离线 BKN**：[`../WORKFLOW-BRANCH-VEGA.zh.md`](../WORKFLOW-BRANCH-VEGA.zh.md) · `worldcup-bkn-vega/` · 可选 `./run-branch-vega.sh`。

常见问题（瘦身宽表 Error 1118、MySQL、`create-from-csv` 超时等）仍以各 README 「故障排查」表为准。
